// Phase 4 stub — browser Supabase client for the public dashboard.
// Uses the ANON key only; RLS (db/schema.sql) keeps this read-only.
// TODO(Phase 4): `npm i @supabase/supabase-js` then uncomment.
//
// import { createClient } from "@supabase/supabase-js";
//
// export const supabase = createClient(
//   process.env.NEXT_PUBLIC_SUPABASE_URL!,
//   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
// );

export {};
